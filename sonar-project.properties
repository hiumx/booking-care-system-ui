# SonarCloud Configuration
sonar.projectKey=Capstone-FA25_booking-care-system-ui
sonar.organization=capstone-fa25-1

# Project Information
sonar.projectName=booking-care-system-ui
sonar.projectVersion=1.0

# Source Configuration
sonar.sources=src
sonar.exclusions=src/assets/**,**/*.test.ts,**/*.test.tsx,**/*.spec.ts,**/*.spec.tsx,**/node_modules/**,**/dist/**,**/build/**

# # Test Configuration
# sonar.tests=src
# sonar.test.inclusions=**/*.test.ts,**/*.test.tsx,**/*.spec.ts,**/*.spec.tsx

# # Coverage Configuration
# sonar.javascript.lcov.reportPaths=coverage/lcov.info
# sonar.coverage.exclusions=src/assets/**,**/*.test.ts,**/*.test.tsx,**/*.spec.ts,**/*.spec.tsx,src/main.tsx,src/vite-env.d.ts,src/declarations.d.ts

# Language Configuration
sonar.typescript.tsconfigPath=tsconfig.json

# Quality Gate Rules
sonar.qualitygate.wait=true

# Duplicate Detection
sonar.cpd.exclusions=src/assets/**
